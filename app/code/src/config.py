"""Configuration for offline training/prediction (no network)."""

from __future__ import annotations

from pathlib import Path
import torch

# ---------------------------------------------------------------------------
# Paths (inside docker)
# ---------------------------------------------------------------------------
DATA_DIR   = Path("/app/data")
MODEL_DIR  = Path("/app/model")
OUTPUT_DIR = Path("/app/output")
TEMP_DIR   = Path("/app/temp")

# ---------------------------------------------------------------------------
# Model architecture
# ---------------------------------------------------------------------------
D_MODEL = 128
N_HEADS = 8
N_GRU_LAYERS = 2
DROPOUT = 0.1
N_TRANSFORMER_LAYERS = 2
D_FF = 256

# ---------------------------------------------------------------------------
# Training
# ---------------------------------------------------------------------------
BATCH_SIZE = 16
N_EPOCHS = 100
LR = 1e-4
WEIGHT_DECAY = 1e-5
GRAD_CLIP = 1.0
LR_PATIENCE = 10
EARLY_STOP_PATIENCE = 25
VAL_RATIO = 0.15
TEST_RATIO = 0.10

# ---------------------------------------------------------------------------
# Data windows
# ---------------------------------------------------------------------------
LOOKBACK_DAYS = 60
PREDICT_HORIZON = 4
STEP_DAYS = 1

# ---------------------------------------------------------------------------
# Feature parameters
# ---------------------------------------------------------------------------
MOMENTUM_WINDOWS = [5, 10, 20, 60]
VOLATILITY_WINDOWS = [5, 10, 20]
MA_WINDOWS = [5, 10, 20, 60]
RSI_PERIOD = 14
MACD_FAST, MACD_SLOW, MACD_SIGNAL = 12, 26, 9
BETA_WINDOW = 60
EXTRA_INDEX_RET_WINDOWS = [5, 10, 20]

# ---------------------------------------------------------------------------
# Event filter
# ---------------------------------------------------------------------------
EVENT_WINDOWS: list[tuple[str, str, str, str]] = [
    ("Russia-Ukraine",   "2022-02-24", "2022-03-09", "俄乌冲突"),
    ("Shanghai Lockdown","2022-03-14", "2022-03-18", "上海封城"),
    ("Lockdown Bottom",  "2022-04-25", "2022-04-29", "封城二次探底"),
    ("H-Share Crash",    "2022-10-24", "2022-10-31", "港股联动暴跌"),
    ("Reopen Frenzy",    "2022-11-28", "2022-12-09", "防疫转向"),
    ("Quant Crash",      "2024-01-29", "2024-02-07", "雪球敲入"),
    ("924 Stimulus",     "2024-09-24", "2024-10-08", "政策转向"),
]

# ---------------------------------------------------------------------------
# Portfolio
# ---------------------------------------------------------------------------
MAX_STOCKS = 5
TOP_K_CANDIDATES = 30

# ---------------------------------------------------------------------------
# Device
# ---------------------------------------------------------------------------
DEVICE = "cuda" if torch.cuda.is_available() else "cpu"
